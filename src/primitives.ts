let  productName:string ="Laptop"
let productPrice:number = 1200.50
let isAvailable :boolean =true
let description:string
description="HP laptop with 1000gb Harddisk and 3 gb RAM"
// productName =false
// Declare a `string` variable `productName` and assign it "Laptop".
// Declare a `number` variable `productPrice` and assign it `1200.50`.
// Declare a `boolean` variable `isAvailable` and assign it `true`.
// Declare a `string` variable `description` but do not initialize it. Then, on the next line, assign it a short description.
// Try to assign `false` to `productName`. Observe the error.
// Compile and run.